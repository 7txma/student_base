<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.studentmanager.mapper.StudentMapper">
    <select id="selectAll" resultType="Student">
        select student_id, name, class_name from student
    </select>

    <insert id="insert">
        insert into student(student_id, name, class_name)
        values(#{studentId}, #{name}, #{className})
    </insert>

    <update id="update">
        update student set name=#{name}, class_name=#{className} where student_id=#{studentId}
    </update>

    <delete id="delete">
        delete from student where student_id = #{studentId}
    </delete>
</mapper>